#ORDER BY- puts columns in order of ascending or descending for strings it is alphabetical
# for numbers in that order from least to most and desc makes it most to least

SELECT * FROM employees
ORDER BY first_name DESC;

SELECT * FROM transactions
ORDER BY amount;
